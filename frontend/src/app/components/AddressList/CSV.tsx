import Image from "next/image";

export const CSV = () => {
  return (
    <div className="h-full flex flex-col gap-4 items-center justify-center">
      <Image src="/image/book.svg" alt="CSV" width={100} height={100}></Image>
    </div>
  );
};
